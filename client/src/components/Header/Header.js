import React from 'react';
import LogoutButton from '../Logout/LogoutButton.js';

const Header = () => {
    return (
        <div>
            Header stuffs
        </div>
    );
};

export default Header;